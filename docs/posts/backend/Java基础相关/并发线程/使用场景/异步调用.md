---
title: 异步调用
date: 2026-06-16
tags: [JUC, 线程池, execute, 异步后置任务]
categories: [后端技术]
description: 线程池execute异步调用实战场景、代码示例、项目多线程三类场景汇总与面试回答
layout: doc
outline: deep
---
# 异步调用
## 一、业务场景说明
### 场景示例：搜索记录异步保存
用户在前端执行搜索操作，主流程仅负责返回搜索结果页面；**用户搜索记录入库**操作不阻塞主业务，交给线程池异步执行。
1. 主线程逻辑：接收搜索关键词 → 查询搜索数据 → 直接返回页面给用户；
2. 异步任务逻辑：在线程池分配独立线程，执行搜索记录入库、热点统计等后置操作。

### 核心价值
无需等待后置IO操作完成，直接返回响应，缩短接口RT，提升用户访问速度。

## 二、适用条件
1. 后置任务**不需要返回结果**，主流程不依赖该任务执行完毕；
2. 后置任务为非核心链路：操作日志、搜索记录、短信/站内信发送、统计埋点、缓存更新等。

## 三、代码示例
```java
// 自定义业务线程池
ThreadPoolExecutor pool = new ThreadPoolExecutor(8, 16, 60, TimeUnit.SECONDS,
                new ArrayBlockingQueue<>(200), new ThreadPoolExecutor.CallerRunsPolicy());

@GetMapping("/search")
public Result<List<ArticleVO>> search(String keyword) {
    // 1. 主线程执行核心查询，立刻返回结果
    List<ArticleVO> result = articleService.search(keyword);

    // 2. 异步提交日志保存任务，不阻塞主线程
    pool.execute(() -> {
        SearchRecord record = new SearchRecord();
        record.setKeyword(keyword);
        record.setCreateTime(new Date());
        searchRecordMapper.insert(record);
        // 额外热点统计、埋点上报
    });
    return Result.success(result);
}
```
使用`pool.execute(Runnable)`，无返回值，纯异步后置处理。

## 四、项目三大多线程场景完整总结
1. **批量导入（线程池 + CountDownLatch）**
   千万级DB数据同步ES，分页分片并发写入，CountDownLatch控制主线程等待全部导入任务完成，一次性加载数据避免OOM。

2. **数据汇总（线程池 + Future）**
   多个无依赖微服务/报表接口并行查询，Future接收各任务返回值，将串行耗时压缩为最慢接口耗时，优化接口响应速度。

3. **异步调用（线程池 execute）**
   无返回值的后置操作（日志、记录、消息推送）异步执行，解除主流程IO阻塞，提升接口吞吐量。

## 五、面试标准回答
问：项目中哪里用到异步多线程？
答：
用户搜索功能中，查询文章列表是核心流程，搜索记录入库属于后置非核心逻辑。我把保存记录的逻辑提交到自定义线程池异步执行，主线程不用等待数据库写入，直接返回搜索结果，减少接口响应耗时。
除此之外还有两类场景：大批量数据同步ES使用CountDownLatch等待全部分片任务完成；订单详情多微服务数据聚合使用Future并行查询，优化串行调用耗时。

---
# ThreadLocal 相关
```markdown
---
title: ThreadLocal 相关
date: 2026-06-16
tags: [JUC, ThreadLocal, ThreadLocalMap, 弱引用, 内存泄漏]
categories: [后端技术]
description: ThreadLocal 作用、API、底层原理、源码流程、内存泄漏成因与解决方案、项目实战场景
layout: doc
outline: deep
---
```
## 一、核心概述
### 1. 核心作用
1. **线程数据隔离**：为每个线程分配独立变量副本，多线程操作互不干扰，从根源解决共享变量并发冲突；
2. **线程内资源共享**：同一线程多处代码可便捷获取同一资源，无需多层传参。
### 2. 典型业务案例
JDBC数据库连接管理：每个线程持有独立Connection存入ThreadLocal，避免多线程共用连接、误关闭其他线程连接。
### 3. 基础三大API
- `set(T value)`：向当前线程存入数据；
- `get()`：读取当前线程绑定的数据；
- `remove()`：清除当前线程绑定的数据，**防止内存泄漏必调用**。

## 二、底层实现原理
### 1. 存储结构
1. 每个`Thread`对象内部持有成员变量 `ThreadLocal.ThreadLocalMap threadLocals`；
2. `ThreadLocalMap` 内部数组 `Entry[] table`，初始容量16；
3. `Entry` 继承 `WeakReference<ThreadLocal<?>>`：
    - key：ThreadLocal实例，**弱引用**；
    - value：业务存储对象，**强引用**。

### 2. set() 源码流程
1. 获取当前线程 `Thread t = Thread.currentThread()`；
2. 获取线程内的ThreadLocalMap；
3. map不为空则 `map.set(this, value)`，以当前ThreadLocal为key存入；
4. map为空则新建ThreadLocalMap并存入键值对。

### 3. get() 源码流程
1. 获取当前线程与对应ThreadLocalMap；
2. 通过ThreadLocal哈希值定位数组下标，取出Entry；
3. 匹配key成功则返回entry.value；无数据返回初始化值。

### 4. remove() 流程
以当前ThreadLocal为key，清除ThreadLocalMap中对应的Entry，释放value强引用。

## 三、内存泄漏问题详解
### 1. 四种引用基础
- 强引用：普通对象引用，GC不会回收，内存不足直接OOM；
- 弱引用：GC扫描到即回收，不考虑内存剩余。

### 2. 泄漏根本原因
1. ThreadLocalMap的key是弱引用，无外部强引用时会被GC回收；
2. value是强引用；
3. 线程池场景线程复用，线程长期存活，key被清空后value无法被GC回收，长期堆积造成内存泄漏。

### 3. 解决方案
使用完ThreadLocal后，**必须在finally块执行remove()**，主动清除key与value，断开强引用。

## 四、完整总结（面试标准回答）
1. 功能：实现线程资源隔离，每个线程拥有独立变量副本，规避并发安全问题，同时在线程内便捷共享资源；
2. 底层：每个Thread持有ThreadLocalMap，以ThreadLocal为key、业务对象为value存储；Entry中key弱引用、value强引用；
3. 操作：set存、get取、remove清；
4. 隐患：线程池长存活线程会导致value无法回收，引发内存泄漏；规范写法使用完毕主动remove释放资源。

## 五、面试简答
问：说说ThreadLocal原理与内存泄漏怎么解决？
答：
ThreadLocal用于线程间数据隔离，每个线程自带ThreadLocalMap，存储结构Entry数组，key是弱引用的ThreadLocal，value是强引用业务对象。调用set/get操作以自身为key读写当前线程数据。
内存泄漏根源：线程池线程长期不销毁，key被GC回收后value强引用无法释放；解决办法是使用完后在finally执行remove()，主动清除Entry。
项目中用于存储数据库连接、登录用户上下文，保证单线程全程共用一份资源，多线程互不干扰。